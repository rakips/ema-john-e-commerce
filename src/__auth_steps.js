/**
 * 1. create a new project console.google.com
 * 2. npm install firebase
 * 3. make firebase.init.js file in src folder and paste the documentation which coppied from sdk doc
 * 4. firebase settings > Authentication > enable email and password auth
 * 
 */






/**
 * hosting steps 
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 */


/**
 * github origin change 
 * 1. For checking primary url  > git remote -v
 * 2. for new url > git remote set-url origin https------
 * 
 */